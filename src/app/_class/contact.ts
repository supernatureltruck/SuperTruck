export class Contact {
    constructor(
        private id: string,
        private email: string,
        private tel: string,

    ) {
        this.id = id;
        this.email = email;
        this.tel = tel;
    }
}
